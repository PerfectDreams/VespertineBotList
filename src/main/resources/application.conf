# Configuração padrão para o frontend da Loritta!
assets {
    cache.maxAge = 31556926
}

server.http.MaxRequestSize = 64MB

# localhost
application.host = 0.0.0.0

session.timeout = 168h
db = "mongodb://10.0.0.3/vespertine_bot_list"